<div id="login" class="ptb ptb-xs-60 page-signin form-register">
<div class="container">
  <div class="row">
    <div class="main-body">
      <div class="body-inner">
        <div class="card bg-white">
          <div class="card-content">
            <section class="logo text-center">
            </section>
            <form {{ attributes }}>
            <p class="description">Sử dụng một Tài khoản, bạn có thể truy cập và sử dụng nhiều hơn các nội dung và tính năng của trang web <a href="">mees.vn</a>.
Bạn có thể đăng ký khóa học, đọc các bài viết đầy đủ, gửi bình luận, đánh giá, mua hàng và theo dõi đơn hàng.</p>
              <h2>Tạo một Tài khoản mới</h2>

              <fieldset>
                <div class="form-group col-6 float-left">
                    <div class="ui-input-group">
                        {{ element.account.mail }}
                    </div>
                </div>
                <div class="form-group col-6 float-left">
                    <div class="ui-input-group">
                    {{ element.field_so_dien_thoai }}
                    </div>
                </div>
                <div class="form-group col-6 float-left">
                    <div class="ui-input-group">
                        {{ element.account.name }}
                    </div>
                </div>
                <div class="form-group col-6 float-left">
                    <div class="ui-input-group">
                    {{ element.field_ho_va_ten }}
                    </div>
                </div>
                <div class="form-group">
                  <div class="ui-input-group">
                    {{ element.account.pass }}
                  </div>
                </div>
                <div class="form-group">
                  <div class="ui-input-group linh-vuc-quan-tam">
                    {{ element.field_linh_vuc_ban_quan_tam }}
                  </div>
                </div>
               
                <div class="spacer"></div>
                <div class="form-group checkbox-field chinh-sach">
                    {{ element.field_toi_dong_y_voi_chinh_sach_ }}
                </div>
                {{ element.actions.submit }}
              </fieldset>
              </div>
             {{ element.form_build_id }} {# required #}
             {{ element.form_id }} {# required #}
            </form>
          
        </div>
      </div>
    </div>
  </div>
</div>